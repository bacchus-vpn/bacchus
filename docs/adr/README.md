# Architecture Decision Records

Short records of significant decisions. Format: **Status, Context, Decision,
Consequences**. Numbered sequentially; superseded records are kept and marked, not
deleted. New significant decisions ship as an ADR in the same PR (see ADR-0007).

## Reading the issue numbers in these records

**ADR numbers are stable and mean one thing. Issue numbers do not.** This project moved
trackers on **2026-07-28**, and every issue open on that day was re-filed with a new
number. References written before the move were left as they were, so `#N` in this
repository belongs to one of two schemes depending on when the sentence was written:

- **Records 0001–0042** arrived in this repository's initial commit. Their original
  bodies were written against the **retired** tracker, so a bare `#N` there is one of
  its numbers.
- **Records 0043 onward** were written here, against the **current** tracker.
- **Amendments appended to an earlier record** are the awkward case, and the reason the
  split is not simply "which file". An amendment introduces current numbers while still
  citing retired ones for the history it builds on, so one record — and sometimes one
  paragraph — carries both. ADR-0038's last amendment cites `#26` in the current scheme
  alongside three numbers in the retired one.

So the boundary runs between passages, not between files, and the two schemes are not
distinguishable by inspection. What tells them apart is the subject matter, not the
number.

**The practical consequence: a retired number below the current tracker's high-water
mark will autolink, and it will point somewhere unrelated.** A record citing `#42` for
admission gating produces a link to a relay-directory change; `#60` for end-to-end exit
verification produces a link to a coordinator-hosting change. So a link in these records
is only as trustworthy as the passage it sits in: where the link and the sentence
disagree, the sentence is right.

Three things worth knowing before trying to "fix" a number:

- **Most retired numbers have no current equivalent.** Only the issues still open on
  2026-07-28 were re-filed. Anything already closed — which is most of what these
  records cite — was not, so there is nothing to renumber it to.
- **`#76` is a label, not just a number.** It was the relay-chaining epic, and the
  current tracker's own issue titles still carry it: `[#76 §9-6] Chain-aware liveness
  and rebuild`, `[#76 §9-9] NAT-traversed intermediate hops`. `#76 §9-N` reads as a
  section of that design, and renumbering it here would desynchronize these records
  from live issue titles.
- **The retired tracker is gone, not merely private.** Its remotes are dead. The only
  surviving record of it is a workspace-local archive that is deliberately never
  published, so a retired number cannot be resolved by anyone outside the project — and
  in many cases the ADR text is now the better record of what the issue said.

The same two-scheme split runs through code comments and the rest of `docs/`, for the
same reason and with the same reading. Comments are inert — nothing autolinks inside a
`.go` file — but a number in one is no more resolvable than a number here.

**Writing a new reference:** a current issue is cited bare, as #77, so it links.
A retired one is cited as `` `old #157` `` — a code span, which GitHub cannot autolink,
so it can never point at an unrelated issue. See [CONVENTIONS.md](../../CONVENTIONS.md).

- [0001](0001-record-architecture-decisions.md) — Record architecture decisions
- [0002](0002-open-monorepo-with-separate-private-payment-repo.md) — Open monorepo, private payment repo
- [0003](0003-single-go-module.md) — Single Go module
- [0004](0004-license-apache-2-0.md) — Apache-2.0 license *(superseded by [0019](0019-relicense-to-agpl-3-0.md))*
- [0005](0005-mobile-shares-the-go-core-via-gomobile.md) — Mobile shares the Go core via gomobile
- [0006](0006-udp-signaling-for-rendezvous-reachability.md) — UDP signaling for reachability
- [0007](0007-documentation-is-part-of-done.md) — Documentation is part of "Done"
- [0008](0008-transport-is-pluggable-webrtc-is-one-implementation.md) — Transport is pluggable, WebRTC is one implementation
- [0009](0009-end-to-end-encryption-client-to-exit.md) — End-to-end encryption client-to-exit
- [0010](0010-full-device-client-not-a-browser-proxy.md) — Full-device client, not a browser proxy
- [0011](0011-v1-scope-is-narrow.md) — v1 scope is narrow
- [0012](0012-cold-start-rendezvous-without-domain-fronting.md) — Cold-start rendezvous without domain fronting
- [0013](0013-bootstrap-wire-protocol.md) — Cold-start bootstrap wire protocol
- [0014](0014-kill-switch-enforced-by-firewall-default-block.md) — Kill-switch enforced by an OS firewall default-block
- [0015](0015-version-policy-and-forced-updates.md) — Version policy and forced updates
- [0016](0016-protocol-version-and-capability-handshake.md) — Protocol version + capability handshake
- [0017](0017-blend-bootstrap-onto-shared-stun-turn-port.md) — Blend bootstrap onto the shared STUN/TURN port
- [0018](0018-randomized-dtls-fingerprint.md) — Randomize the WebRTC DTLS ClientHello fingerprint
- [0019](0019-relicense-to-agpl-3-0.md) — Relicense to AGPL-3.0 (supersedes 0004)
- [0020](0020-coordinator-pool-and-client-rotation.md) — Coordinator pool + client rotation
- [0021](0021-co-signed-usage-receipts-stub.md) — Co-signed usage receipts (metering stub, no payout)
- [0022](0022-reduce-residual-ice-and-serverhello-tells.md) — Reduce residual WebRTC ICE / DTLS-ServerHello tells (extends 0018)
- [0023](0023-cryptographic-node-admission.md) — Cryptographic node & client admission (signed membership credentials)
- [0024](0024-second-transport-reality-tcp.md) — Second transport: Reality over TCP :443 (implements 0008)
- [0025](0025-destination-based-split-tunnelling.md) — Destination-based split tunnelling (exclusion routes + live-learned bypass domains)
- [0026](0026-end-to-end-exit-admission-verification.md) — Client verifies the exit's admission credential end-to-end (extends 0023)
- [0027](0027-reality-active-probing-response.md) — Reality active-probing response: reverse-proxy unknown connections to a real origin (follow-up to 0024)
- [0028](0028-transport-pool-and-per-user-failover.md) — Transport/exit selection is a validated priority ladder with per-network learning (drives 0008, 0024)
- [0029](0029-version-fencing-implementation.md) — Min-version fencing: implementation of the version policy (implements 0015)
- [0030](0030-auto-reconnect-and-relay-failover.md) — Auto-reconnect + relay failover on the single-transport path (non-pooled counterpart to 0028)
- [0031](0031-first-class-session-and-peer-expiry.md) — First-class session & peer expiry: engine idle-reaper + activity-based coordinator TTLs (server-side counterpart to 0030)
- [0032](0032-reality-borrowed-certificate-clienthello-fork.md) — Reality borrowed certificate: authenticate in the ClientHello, fork unauthenticated connections to the real origin (follow-up to 0024, extends 0027)
- [0033](0033-prefer-peer-relays-over-turn-for-the-data-plane.md) — Prefer peer relays over TURN for the data plane: coordinator hands a peer-relay candidate, TURN as fallback (refines 0028's node tier)
- [0034](0034-udp-relay-forwarding.md) — General UDP relay forwarding rides the same E2E channel as CONNECT, via a target sentinel
- [0035](0035-surface-assigned-relay-to-client-for-rotation-dedup.md) — Surface the assigned relay to the client as an opaque tag for rotation dedupe (extends 0020, follows 0033)
- [0036](0036-windows-client-connection-strategy-and-invite-qr-ui.md) — Windows client gets a real GUI (lxn/walk) for connection-strategy settings (drives 0028) and invite QR display
- [0037](0037-mesh-walk-recovery-via-signed-peer-exchange.md) — Mesh-walk recovery: relay/exit couriers serve a cached coordinator-signed snapshot to a client that lost all coordinators (warm re-bootstrap, generalizes 0020; shares the snapshot with issue #6)
- [0038](0038-configurable-multi-hop-relay-chaining.md) — Configurable multi-hop relay chaining: client-assembled onion of nested Noise_NK layers so no single relay sees both endpoints, default 1 hop == today (generalizes 0033, lives in 0028's relay tier, preserves 0009/0026 E2E)
- [0039](0039-cross-platform-fyne-client-in-process-core.md) — Cross-platform client: all-Go Fyne UI calling core in-process, no webview, no FFI bridge (spike-proven seam; new cgo/mingw-w64 build prerequisite); amended by #152/#153 (settings window, continuous Linux build verification) and by #35 — Fyne becomes the client on all three desktop platforms and `clients/windows` retires only once a platform clears an eight-point written parity bar (full-device routing both split modes, fail-closed kill-switch, crash recovery, live allowlist refresh, pool-underlay exclusion ordering, IPv6, documented elevation, and a traffic-level test), with `clients/internal/enforcement` as the one seam `[E9]`/`[E10]` implement against and #16 `[E3]` built in Fyne
- [0040](0040-node-capacity-declared-limits-and-attested-measurement.md) — Node capacity: declared limits are self-reported (lying is self-limiting), measured capacity never is (lying pays) — usable = min(declared, measured); measurement is serving, not testing, so there is no tester. #143 built; #144 is a spike whose Sybil residual is stated openly (extends 0021, priced by 0023)
- [0041](0041-attested-capacity-feed-and-two-rating-estimator.md) — Attested-capacity feed + coordinator two-rating estimator: a co-signed receipt + one client-asserted saturation bit feeds a per-node `trusted`/`untrusted` pair (trusted decides, untrusted clamped to the ceiling); `Usable` at the assignment surfaces with the serve floor shipped off; the reality splice is now metered too (implements #157/#158, wires 0040, amends 0021 and 0027)
- [0042](0042-country-only-exit-assignment.md) — Country-only exit assignment: the coordinator derives a node's country from the address it OBSERVED (never the node's `-country` claim), the client names a country rather than an exit (killing a stable tracking handle), and a country with no headroom is reported busy rather than as a bare error (implements #136/#146/#147, amended by #1/#2/#3)
- [0043](0043-coordinator-enforces-signed-network-policy.md) — The coordinator enforces signed network policy it cannot author: a general `bacchus/delegation/v1` verifier (role matched exactly, reusable for the update/relay roles), policy `min_serving_version` overriding the flag in one place, a persisted sequence floor and byte-cached bundle re-verified on load, and a fail-closed soft drain past `exp + grace` that stops new assignments and leaves live sessions alone (implements #39, turns on what 0041 shipped off); amended twice — #15, the serve floor reads `min_measured_bps` from the policy with no constant default, applied to the TRUSTED rating only, since a floor on the ceiling-clamped untrusted rating would fence every measured node and admit every unmeasured one; then #67 reverses §6's `tiers` half — the limits live in the signed policy and the credential carries only the `(trust, plan)` pair that indexes them, so the network resolves what it enforces out of a document it verified rather than enforcing a number stamped into a credential under a policy it cannot see, and a re-signed policy takes effect without waiting for every outstanding credential to age out. An unknown pair stays an ERROR, never a default, because a zero `TierLimit` reads as *uncapped* — a permissive fallback would open exactly when someone ships a plan and forgets the policy. `vouch` is untouched and still unread here; producer side is `bacchus-payment#6`, consumer side is #58
- [0044](0044-as-lookup-seam-and-as-diverse-hop-selection.md) — An independent IP→AS lookup behind one seam (`core/asn.Lookup`), AS-diverse hop selection derived from each hop's OBSERVED address rather than any signed tag, and the coordinator's `observedAS` moved behind the same interface with the prefix mask kept as the NAMED fallback; an unresolved AS is accepted but counts no diversity (never diverse-from-everything) and the degradation is reported rather than silent (implements #23, unblocks the ADR-0041 line 173 follow-up); amended twice — §6's open distribution question is ruled option A, the table is embedded in the client build and refreshed per release, committed rather than fetched at build time so the build stays reproducible for #34/#38, with a scheduled fetch rejected on the measured ~1.3%/month drift not being worth a fingerprintable periodic request from every client; then the bytes ship (#55) — the encoding is measured at 3.14 MB gzipped TEXT through the existing parser (against 1.38 MB for a delta-varint form that would have put a custom decoder in front of a security-relevant parse, so text stands), the source is confirmed at the source as iptoasn.com's combined feed under PDDL v1.0 (public domain, redistribution permitted, which is what makes committing it to an AGPL repo sound — the deciding factor is licence, not size), `cmd/asn-stage` lands as the reviewable deterministic refresh transform, and the table is wired in at `loadRelayDirectory` rather than through engine config because embedding is not configuration; §6's IPv4-only size lever is declined on the same measurement (0.65 MB saved to blind the control on every IPv6 hop — both families ship), and the per-release refresh gains a floor under it via `TableRetrieved` + a CI staleness test at 90 days, checked in CI rather than at client startup because only whoever cuts a release can act on it
- [0045](0045-connect-time-device-credential-verification.md) — Connect-time device-credential verification: the coordinator verifies the account service's two-tier chain (offline root → issuer cert → device credential) OFFLINE at connect, tier by tier with the issuer cert fully validated before its key is used, the `max_cred_ttl` cap bound at verification, and a single-use coordinator-chosen challenge bound to an audience the client knows independently; coexists with `core/admission` rather than replacing it (different authorities, different questions, both checked), and fails OPEN when unconfigured — the opposite of 0043, because an absent anchor is read identically by every coordinator and so has nothing to shed to (implements #50, closes R1)
- [0046](0046-client-answers-the-device-credential-gate.md) — The client answers 0045's gate: one chokepoint in `attemptWith` builds the challenge/sign/connect exchange, the challenge REQUEST is sent once rather than via `sendN` (its store replaces-on-issue, unlike the idempotent connect it precedes), a missing or empty challenge degrades to a plain connect rather than blocking the attempt, `core/devicestore` splits a hard-fail identity (the on-device key) from a soft-fail cache (the renewable credential), and renewal is a signing primitive plus a pluggable seam rather than a built-in client for an account-service HTTP contract that does not exist yet anywhere (implements #53); `cmd/node`'s flag is proposed, not applied, across this change's file-ownership boundary
- [0047](0047-role-scoped-admission-authority-set.md) — Admission anchors a role-scoped SET of authorities rather than one key: an `Authority` pairs a public key with the roles it may admit (the anchor's claim, unreachable from any signing key, as against the credential's own `roles` field which the issuer writes freely), the set is filtered by the requested role BEFORE any signature is verified so the scoping is structural rather than a check that could be reordered away, and a wrong-authority credential consequently returns `ErrBadSignature` instead of `ErrRoleNotAuthorized` — the oracle reasoning 0045 used, arrived at as a side effect of the ordering. `-admission-pubkey` keeps its exact meaning (one authority, every role) with a repeatable `-admission-authority role[,role...]:hexkey` alongside it, flags rather than a hot-reloaded file because a bad write to an anchor widens who may join while a bad write to the revocation list only fails safe; `NewVerifier` stays single-key because the client's anchor genuinely is one key (its only check is `RoleExit`, and a coldstart invite carries one `AdmissionKey`), which also keeps `core.Config.AdmissionPubKey` a scalar (implements #64, deploys bacchus-payment#38's separate account-service key, closes R1's last code card; `cmd/node`'s matching help-text note is proposed, not applied, across the file-ownership boundary)
- [0048](0048-tier-limits-resolved-from-signed-policy.md) — Tier limits are resolved from the signed policy and enforced by the network (consumer half of 0043's #67 amendment, implements #58): the credential gains `trust` + `plan` declared immediately after `vouched` — declaration order is the cross-repo signed-body contract, proven by running `bacchus-payment`'s 18 frozen vectors plus a re-marshal round trip through this repo's verifier from a throwaway module, since verification alone is order-blind. The coordinator enforces `priority` (scaling `minShare`, inert while that ships at zero) and `endpoint_quality` (a floor on a coordinator-observed class, unfed and therefore withholding nobody — an unclassified endpoint is not a bad endpoint, the same shape as #15's serve floor, and treating it as class 0 would refuse the whole fleet under any realistic signed policy); the exit shapes to `speed_cap_bps` from a number carried on the assignment. The exit is never shown the credential and never fetches policy: a `Serial` is a stable per-device identifier and the exit is the one party that sees the user's traffic, so re-presenting a credential there is the session-linking join key 0042/0035 exist to deny — the price is that a hostile coordinator can hand out an uncapped session, a revenue-integrity failure rather than 0020's linkability one, bounded by the node's own declared cap under 0040. Three fail directions decided separately (0043 and 0045 reached opposite answers on the same test): no policy configured assigns unshaped, an unknown `(trust, plan)` pair refuses loudly per 0006 decision 5 — so every pre-#58 credential is refused once policy is on — and policy-with-admission-off assigns unshaped but warns at startup. Per-session shaping covered the direct path only at first; amended by #74 — a peer-relayed session is now shaped at the RELAY, which terminates nothing but forwards every byte, so §5's "a relay could not shape the session" was a false premise; the exit is told nothing, so §4's linkability property is untouched and the relay learns only a coarse number it could already derive by measuring what it forwards. The UDP relay paces per datagram with `WaitN`, which needs no reader to wrap, keeping `meterN` beside it since the two answer different questions. A **chained** connect stays unshaped, as a separate gap with a separate reason — the coordinator does not know where a client-assembled path terminates (ADR-0042 §9)
- [0049](0049-linux-privilege-boundary.md) — The Linux privilege boundary for #37 `[E10]`: enforcement splits across a process boundary, with `clients/fyne` unprivileged and a root helper (`bacchus-netd`) behind a `SO_PEERCRED`-gated unix socket owning all thirteen privileged `osNet` methods — chosen over file capabilities (an ambient grant, lost by `cp`/`nosuid`/upgrade, and exec-per-op leaves the rollback of a security boundary in the untrusted process, which is what `TestFailedBringUpOrphansNothing` pins) and over polkit (ADR-0030's auto-reconnect makes every failover a password prompt, and `allow_active=yes` buys a launcher rather than a boundary; polkit's real fit is a one-time install-time grant). The decisive argument is the TUN descriptor: the netstack runs in the client process, and a unix socket is the only one of the three that can pass an fd — the vendored wireguard-go already splits exactly there, `CreateTUN` doing the privileged `open`+`TUNSETIFF` and `CreateTUNFromFile` doing only unprivileged work on a descriptor it is handed. The helper speaks netlink and nftables directly and never shells out, so no client string can become an instruction; only prefixes, an address and a session token cross inward, while interface names and `gatewayInfo` are derived helper-side and cross outward only (verified: `osn.defaultGateway` is the package's sole producer — `tunnel.go:79`, `poolroutes.go:114`/`:230`). The security claim is stated without inflation: a compromised GUI can leak traffic and lift the kill-switch, because the GUI is what decides policy, but cannot become root, touch state outside the helper's own table, or persist past a reboot — compromise costs the VPN, not the machine, and the kill-switch is consequently no defence against local malware. Corrects the card on two points: the split-tunnel "mechanism decision" (routing rules / cgroup v2 / netns) **is not open** — `splittunnel.go` is destination-based, portable and shipped, its only decision function takes an IP, and ADR-0025 already ruled per-app split tunnelling out of scope, so what remains is three route methods and nothing to decide; and DNS is where `osNet` is genuinely short a primitive, since systemd-resolved's `127.0.0.53` stub is loopback that **no** route can capture (the kernel's `local` table precedes `main`), which breaks bypass-domain learning and gives a leak with the kill-switch off or total DNS failure with it armed. Records the full mechanism/privilege map for all fourteen methods, that `refreshKillSwitchAllowIP` has no fails-closed window on Linux (nftables named sets take an atomic element add, so ADR-0025's remove-and-recreate must not be ported), and that `tunnel_test.go`'s five pinned orderings are inherited structurally rather than re-established — so the helper must execute primitives and never own bring-up order — with a user+network namespace giving #37 the real-kernel, unprivileged CI verification #59 never had (a netns as test harness, explicitly not as split-tunnel mechanism). Design half only; #37 stays open
