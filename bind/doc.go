// Package bind is the gomobile-safe facade over core, exposing a minimal API
// (simple types only — strings, ints, []byte, and interfaces with methods) for
// Kotlin/Swift. It is the only package gomobile compiles; the rich engine lives
// in core. Placeholder until the core extraction lands.
package bind
